# Integration tests

Integration test suite for the drivers which common interface is defined [here](./plugin.hpp).

> **IMPORTANT**: The CMake variable `STORAGE_DRIVER_TARGET_NAME` must be defined prior to running CMake.